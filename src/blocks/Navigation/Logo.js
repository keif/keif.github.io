import styled from "styled-components";
import { Link } from "gatsby";

const Logo = styled(Link)`
  line-height: 0;
  img {
    width: 140px;
    height: auto;
    margin-bottom: 0;
  }
`;

export default Logo;
